{
    "name": "RE_MaxPoolTest_taxonomy",
    "n_gpu": 1,

    "arch": {
        "type": "MaxPoolTest",
        "args": {
            "num_classes": 2,
            "num_embeddings": 60000,
            "embedding_dim": 100,
            "max_sent_length": 256,
            "hidden_size": 120
            
        }
    },
    "data_loader": {
        "type": "RelationDataLoader",
        "args":{
            "data_dir": "../data/relation_extraction",
            "batch_size": 1,
            "shuffle": true,
            "validation_split": 0.1,
            "num_workers": 0,
            "relations": ["subclass-of"],
            "embedding_type": "custom",
            "split": "debug",
            "max_sent_length": 256
        }
    },
    "optimizer": {
        "type": "Adam",
        "args":{
            "lr": 0.001,
            "weight_decay": 0,
            "amsgrad": true
        }
    },
    "loss": "nll_loss",
    "metrics": [
        "accuracy", "macro_f1", "micro_f1", "micro_precision", "macro_precision", 
        "micro_recall", "macro_recall"
    ],
    "lr_scheduler": {
        "type": "StepLR",
        "args": {
            "step_size": 50,
            "gamma": 0.1
        }
    },
    "trainer": {
        "epochs": 20,

        "save_dir": "saved/",
        "save_period": 1,
        "verbosity": 2,
        
        "monitor": "min val_macro_f1",
        "early_stop": 0,

        "tensorboard": true
    }
}
